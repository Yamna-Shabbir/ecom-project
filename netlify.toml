# Netlify settings for the React frontend (Create React App)
[build]
  base = "frontend"
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# React Router — all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
